#include <iostream>
#include <bitset>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

#define REP(i,n) for(int i=0;i<n;i++)

int main()
{
    int A, B, C, D, E;
    cin >> A >> B >> C >>D >> E;

    int MinMove = min({A, B, C, D, E});

    //cout << ans << endl;
}